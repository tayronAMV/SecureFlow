להריץ את הקלאסטר
minikube start --driver docker 

להריץ את הפודים 
kubectl apply -f <filename>.yaml

לעשות מאונט לתיקייה של הפרויקט
minikube mount $(pwd):/mnt/secureflow - in the secureflow root dir

לקמפל ולעשות בילד לגו בשביל הרצה בקונטיינר של המיניקיוב
go build -o agent ./cmd/agent

איך לדחוף שינויים לגיט
git add .
git commit -m "your message here"
git push

